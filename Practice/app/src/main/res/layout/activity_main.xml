<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="mao.practice.MainActivity">

    <EditText
        android:id="@+id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/edit_name"/>

    <EditText
        android:id="@+id/number"
        android:layout_below ="@id/name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/edit_text"/>

    <EditText
        android:id="@+id/email"
        android:layout_below="@id/number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:hint="@string/edit_email"/>

    <Button
        android:id="@+id/save_button"
        android:layout_below="@id/email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/click"
        android:onClick="saveOnClick"/>

    <Button
        android:id="@+id/view_raw_button"
        android:layout_toRightOf="@id/save_button"
        android:layout_below="@id/email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/view_raw"
        android:onClick="view_raw_OnClick"/>

    <Button
        android:id="@+id/view_list_button"
        android:layout_toRightOf="@id/view_raw_button"
        android:layout_below="@id/email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/view_list"
        android:onClick="view_list_OnClick"/>


</RelativeLayout>
